[package]
name = "glfw3"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "0.2"
pkg-config = "0.3.20"

[target.'cfg(unix)'.build-dependencies]
pkg-config = { version = "0.3.20" }